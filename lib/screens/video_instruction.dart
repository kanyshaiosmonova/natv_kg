import 'package:flutter/material.dart';

class VideoInstructionWidget extends StatelessWidget {
  const VideoInstructionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
