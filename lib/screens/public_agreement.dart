import 'package:flutter/material.dart';

class PublicAgreementWidget extends StatelessWidget {
  const PublicAgreementWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
